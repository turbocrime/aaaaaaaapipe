{
  "name": "aaaaaaaapipe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fly": "tsc && vite build && fly deploy"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.10",
    "@vitejs/plugin-basic-ssl": "^1.0.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.3"
  }
}